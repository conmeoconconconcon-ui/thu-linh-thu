name: thu_linh_thu
description: Strong EPUB text reader
publish_to: "none"

version: 1.0.0+7

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.0.0
  archive: ^3.6.1
  xml: ^6.5.0
  flutter_html: ^3.0.0-beta.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
