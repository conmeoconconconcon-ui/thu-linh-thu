name: thu_linh_thu
description: Kindle + Pet demo
publish_to: "none"

version: 1.0.0+8

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.0.0
  archive: ^3.6.1
  xml: ^6.5.0
  shared_preferences: ^2.2.3
  html_unescape: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
